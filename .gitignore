# segredos — use variáveis de ambiente (Coolify), nunca versione a chave
.env
.env.shared
.env.local

# dependências / lixo
node_modules/
*.log
.DS_Store
.claude/

# artefatos de desenvolvimento, fontes brutas e backups (ficam só na máquina)
_dev/
uploads/
